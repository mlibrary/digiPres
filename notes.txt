Build process for openshift

docker buildx build \
    --pull \
    --no-cache \
    --progress=plain \
    --push \
    --tag ghcr.io/mutanthumb/baby-blue:multipatch.1 \
    --platform linux/arm64,linux/amd64 \
    .
    
    
    
    
oc exec deployment/baby-blue -- \
  python manage.py migrate
  
  
Activate venv:
source ./venv/bin/activate 


Image name:
baby-blue:multipatch.12