Build process for openshift

docker buildx build \
    --pull \
    --no-cache \
    --push \
    --tag ghcr.io/mutanthumb/baby-blue:multi.4 \
    --platform linux/arm64,linux/amd64 \
    .
    
    
    
    
oc exec deployment/baby-blue -- \
  python manage.py migrate